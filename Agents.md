\# SIDELOADOS: MASTER AGENT DIRECTIVES



\## 1. THE "INCEPTION" PROTOCOL

You are an elite autonomous agent operating within Google Antigravity. We are building SideloadOS—an open-source, web-based AI Orchestration Operating System.

\*\*CRITICAL:\*\* SideloadOS is designed to look and function EXACTLY like your own Google Antigravity Agent Manager. It features a 3-pane layout, Walkthrough Cards, and HITL (Human-In-The-Loop) Artifacts. Do not confuse your \*actual\* Antigravity capabilities with the \*React/FastAPI source code\* you are writing. 



\## 2. ANTIGRAVITY EXECUTION RULES

\- \*\*ALWAYS Use Planning Mode:\*\* Never write code blindly. Always generate an `Implementation Plan Artifact` first. Wait for my approval before executing file creation.

\- \*\*Produce Verifiable Artifacts:\*\* When building the Next.js frontend, you MUST spawn the Browser Sub-Agent to test the UI on `localhost:3000`. Produce a `Walkthrough Artifact` with verification that the 3-pane layout resizes correctly.

\- \*\*Isolate Your Work:\*\* Always check the `docs/` folder to ensure strict alignment between the frontend and backend API contracts.

